# marketing/

The Sequence Bridge marketing site — sells the service to dental practices.

Static site, deploys independently of the app so copy changes never require an app deploy. Uses the Sequence Bridge brand theme from `internal/design/tokens.json`, with persuasion-oriented components (hero, pricing, testimonials, FAQ, CTA).

Not yet built. Spec it via the `internal-scaffolding` skill before implementation.
