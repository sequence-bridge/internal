# Roadmap

Planned work, grouped by the month it's targeted for. Checkbox bullets ΓÇö `- [ ]` for not yet done, `- [x]` for done. Undated ideas live in `## Backlog` at the bottom.

To start work on an item, ask the `internal-scaffolding` skill to spec it ΓÇö that triggers Phase 2 and creates a file in `internal/specs/`.

When a month wraps, run the archive command (e.g., "archive 2026-07") to move every `- [x]` item under that heading into `internal/releases/roadmap-log.md`. Unchecked items stay where they are; the heading is removed only if everything moved.

## 2026-07
- [ ]


## 2026-08
- [ ] 

## Backlog
- [ ]
- [ ]
